# Connected Knowledge Base Adapter

Follow [AGENTS.md](AGENTS.md). For durable knowledge-base work, load
`$maintain-llm-wiki` — it is a trigger: call `kb_overview` and follow the
gateway.

The configured Knowledge Gateway is the live authority. Knowledge content and
backups belong in separate storage, not in this project.
