---
description: Connected cloud-first knowledge-base policy
alwaysApply: true
---

Follow `/AGENTS.md` and `/.agents/rules/knowledge-base.md`.

For durable knowledge work, load `$maintain-llm-wiki` (trigger). Call
`kb_overview` and follow the gateway `usage` and `taxonomy`. Keep live
knowledge, backups, credentials, and private runtime configuration outside
this project.
